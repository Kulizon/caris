Metadata-Version: 2.4
Name: caris
Version: 0.1.0
Summary: CARIS - Iconclass symbol classification and recommendation from images.
Requires-Python: >=3.12
Description-Content-Type: text/markdown
Requires-Dist: iconclass==0.28
Requires-Dist: ultralytics
Requires-Dist: transformers==4.49.0
Requires-Dist: einops
Requires-Dist: timm
Requires-Dist: chromadb
Provides-Extra: gemma
Requires-Dist: ollama; extra == "gemma"
Provides-Extra: ram
Requires-Dist: torch; extra == "ram"
Requires-Dist: pillow; extra == "ram"
Provides-Extra: scrape
Requires-Dist: requests; extra == "scrape"
Requires-Dist: beautifulsoup4; extra == "scrape"
Requires-Dist: tqdm; extra == "scrape"
Provides-Extra: embeddings
Requires-Dist: ollama; extra == "embeddings"
Requires-Dist: requests; extra == "embeddings"
Requires-Dist: tqdm; extra == "embeddings"

# CARIS - Klasyfikacja i Rekomendacja Symboli Iconclass

Projekt służy do automatycznej analizy obrazów, detekcji obiektów/tagów za pomocą różnych modeli uczenia maszynowego, a następnie przypisywania im odpowiednich kodów klasyfikacji **Iconclass** przy użyciu wyszukiwania strukturalnego lub bazy wektorowej (embeddings).

---

## 🛠️ Konfiguracja początkowa (Setup)

Aby uruchomić projekt na systemie Windows, wykonaj poniższe kroki w swoim terminalu:

### 1. Środowisko wirtualne i podstawowe zależności
Zaleca się używanie Pythona **3.13** (lub 3.12). Stwórz środowisko wirtualne, aktywuj je i zainstaluj wymagane pakiety:

```bash
# Utworzenie środowiska wirtualnego
python -m venv .venv

# Aktywacja środowiska wirtualnego (PowerShell / CMD)
.venv\Scripts\activate

# (Mac Linux)
source .venv/bin/activate

# Aktualizacja pip
python -m pip install --upgrade pip

# Instalacja podstawowych bibliotek (w tym zoptymalizowanej wersji transformers dla Pythona 3.13)
pip install -r requirements.txt

# Instalacja pakietu CARIS w trybie edytowalnym (umożliwia uruchamianie przez `python -m caris...`)
pip install -e .
```

### 2. Instalacja i konfiguracja RAM++ (Recognize Anything)
Model **RAM++** wymaga sklonowania jego oficjalnego repozytorium i instalacji w trybie edytowalnym:

```bash
# Klonowanie repozytorium modelu
git clone https://github.com/xinyu1205/recognize-anything.git

# Wejście do folderu i instalacja w trybie deweloperskim (-e)
cd recognize-anything
pip install -e .
cd ..
```

> [!NOTE]
> Plik checkpointu modelu RAM++ (`ram_plus_swin_large_14m.pth` o rozmiarze ok. 3 GB) zostanie **automatycznie pobrany** z Hugging Face podczas pierwszego uruchomienia detekcji i zapisany w cache systemu. Nie trzeba go pobierać ręcznie!

### 3. Konfiguracja bazy wektorowej (ChromaDB) i Ollama
Dla modeli korzystających z wyszukiwania semantycznego (**Gemma** oraz **RAM**), system mapuje słowa kluczowe na kody Iconclass przy użyciu lokalnej bazy wektorowej ChromaDB.

1. Upewnij się, że masz zainstalowane i uruchomione środowisko **Ollama**.
2. Pobierz wymagany model osadzeń (embeddings):
   ```bash
   ollama pull mxbai-embed-large
   ```
3. (Opcjonalnie dla trybu Gemma) Pobierz model językowy Gemma:
   ```bash
   ollama pull gemma4:12b  # lub inną wersję 
   ```
4. Utwórz lokalną wektorową bazę kodów Iconclass, uruchamiając skrypt narzędziowy:
   ```bash
   python -m caris.embeddings
   ```
   *Skrypt pobierze definicje Iconclass, wygeneruje dla nich embeddingi i zapisze lokalną bazę ChromaDB w folderze `data/iconclass_db`.*

---

## 🤖 Dostępne modele detekcji

W pakiecie `caris.detection` zintegrowana jest funkcja `detect_objects_in_image(trained_model, image_name)`. Automatycznie rozpoznaje ona wybrany model na podstawie jego nazwy:

| Model | Identyfikator w `--model` | Metoda mapowania na Iconclass | Charakterystyka |
| :--- | :--- | :--- | :--- |
| **YOLO** | Ścieżka do pliku `.pt` (np. `models/yolo26n.pt`) | Rekurencyjne przeszukiwanie drzewa | Szybka detekcja obiektowa ograniczona do podstawowych klas (np. człowiek, pies, koń). |
| **Gemma** (Ollama) | Dowolna nazwa zawierająca `"gemma"` (np. `gemma`) | Wyszukiwanie wektorowe (ChromaDB) | Model VLM wyciągający bogaty opis sceny i symboliki za pomocą promptu tekstowego. |
| **RAM++** (Recognize Anything) | Dowolna nazwa zawierająca `"ram"` (np. `ram` lub `ram_plus`) | Wyszukiwanie wektorowe (ChromaDB) | Wyjątkowo dokładny i szybki model tagujący typu open-vocabulary. Zwraca szeroki wachlarz tagów bez halucynacji. |

---

## 📈 Uruchamianie ewaluacji (Evaluation)

Ewaluację modeli przeprowadza się za pomocą modułu `caris.evaluation.evaluate`. Uruchamia on detekcję na obrazach testowych z katalogu `eval_data/images` i porównuje wyniki z rzeczywistymi kodami Iconclass z pliku `eval_data/data.json`.

### 1. Ewaluacja za pomocą modelu YOLO
```bash
python -m caris.evaluation.evaluate --model models/yolo26n.pt
```

### 2. Ewaluacja za pomocą modelu Gemma (Ollama)
```bash
python -m caris.evaluation.evaluate --model gemma
```

### 3. Ewaluacja za pomocą modelu RAM++
```bash
python -m caris.evaluation.evaluate --model ram
```

### ⚙️ Przydatne parametry modułu `caris.evaluation.evaluate`:
* `--mapping <auto|structural|semantic>` - Określa sposób mapowania wykrytych obiektów/tagów na kody Iconclass (domyślnie: `auto`):
  * `auto` - automatyczny dobór (YOLO korzysta ze `structural`, Gemma oraz RAM++ z `semantic`).
  * `structural` - dopasowanie ścisłe i podzbiorów w hierarchicznym drzewie słów kluczowych Iconclass.
  * `semantic` - semantyczne wyszukiwanie wektorowe w lokalnej bazie wektorowej ChromaDB (embeddings).
* `--max-images <liczba>` - Ogranicza liczbę obrazów do ewaluacji (doskonałe do szybkich testów, np. `--max-images 5`).
* `--eval-dir <ścieżka>` - Umożliwia wskazanie innego katalogu ze zbiorem ewaluacyjnym (domyślnie: `eval_data`).

### 💡 Przykłady niestandardowych kombinacji (6 możliwości)
Dzięki rozdzieleniu modelu detekcji od metody mapowania możesz uruchomić dowolne z 6 połączeń, na przykład:
```bash
# YOLO z wyszukiwaniem wektorowym (semantycznym) zamiast hierarchicznego
python -m caris.evaluation.evaluate --model models/yolo26n.pt --mapping semantic

# RAM++ z wyszukiwaniem hierarchicznym (strukturalnym) zamiast wektorowego
python -m caris.evaluation.evaluate --model ram --mapping structural
```

---

## 💡 Przykłady szybkiego testu detekcji

Możesz szybko przetestować samą detekcję na wybranym obrazku (np. `polowanie.jpg`) za pomocą skryptu testowego:

```python
from caris.detection import detect_objects_in_image

# Uruchomienie detekcji RAM++
tagi = detect_objects_in_image("ram", "polowanie.jpg")
print("Wykryte obiekty:", tagi)
```
